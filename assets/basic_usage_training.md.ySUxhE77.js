import{_ as a,o as s,c as i,a8 as t}from"./chunks/framework.DpsX1MbM.js";const g=JSON.parse('{"title":"Training","description":"","frontmatter":{},"headers":[],"relativePath":"basic_usage/training.md","filePath":"sections/basic_usage/training.md","lastUpdated":1788846900000}'),n={name:"basic_usage/training.md"};function l(r,e,o,d,h,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="training" tabindex="-1">Training <a class="header-anchor" href="#training" aria-label="Permalink to &quot;Training&quot;">​</a></h1><p>SpecForge has one public training entry point for every strategy and runtime topology:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/external/qwen3-8b-eagle3-disaggregated.yaml</span></span></code></pre></div><p>The YAML file is the run contract. It selects the draft strategy, target model, data source, optimizer settings, and deployment topology. Method-specific Python trainers are not part of the public interface.</p><p>This is an intentional hard cutover. The old <code>scripts/train_*.py</code> commands and temporary move-only Python import paths were removed rather than deprecated. Downstream launchers should migrate to a typed run config and <code>specforge train</code>; there is no compatibility dispatch to the previous trainers.</p><h2 id="choose-a-recipe" tabindex="-1">Choose a recipe <a class="header-anchor" href="#choose-a-recipe" aria-label="Permalink to &quot;Choose a recipe&quot;">​</a></h2><p>The config catalog separates three concepts:</p><table tabindex="0"><thead><tr><th>Question</th><th>Config source</th><th>Catalog level</th></tr></thead><tbody><tr><td>Are target features captured during training or prepared earlier?</td><td><code>data.train_data_path</code> / <code>data.prompts_path</code> versus <code>data.hidden_states_path</code></td><td><code>online/</code> versus <code>offline/</code></td></tr><tr><td>Does the trainer read offline hidden states files directly or consume refs from a producer?</td><td><code>deployment.mode: local_colocated</code> versus <code>deployment.mode: disaggregated</code></td><td><code>colocated/</code> versus <code>disaggregated/</code></td></tr><tr><td>Who starts Mooncake and SGLang for online disaggregation?</td><td>Presence of <code>deployment.disaggregated.managed_local</code></td><td><code>external/</code> versus <code>managed-local/</code></td></tr></tbody></table><p>The supported catalog layout is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>examples/configs/</span></span>
<span class="line"><span>├── offline/</span></span>
<span class="line"><span>│   ├── colocated/</span></span>
<span class="line"><span>│   └── disaggregated/</span></span>
<span class="line"><span>└── online/</span></span>
<span class="line"><span>    ├── colocated/                 # reserved; currently unsupported</span></span>
<span class="line"><span>    └── disaggregated/</span></span>
<span class="line"><span>        ├── external/</span></span>
<span class="line"><span>        └── managed-local/</span></span></code></pre></div><p><code>external</code> is an ownership boundary, not a statement that the services are on another machine. It is <code>external</code> when the user starts the server. <code>managed-local</code> owns those services on one host, while producer and consumer remain separate SpecForge roles. The runtime reads the YAML fields, not filename suffixes, to determine these semantics. See the complete <a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/README.md" target="_blank" rel="noreferrer">recipe catalog</a> for representative configs.</p><h2 id="launch-a-run" tabindex="-1">Launch a run <a class="header-anchor" href="#launch-a-run" aria-label="Permalink to &quot;Launch a run&quot;">​</a></h2><p>Use the command directly for every checked-in topology:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/external/qwen3-8b-eagle3-disaggregated.yaml</span></span></code></pre></div><p><code>deployment.trainer.nproc_per_node</code> records the audited local process count. When it is greater than one, the CLI starts torch distributed itself:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/external/qwen3-30b-a3b-eagle3-online.yaml</span></span></code></pre></div><p>Online target inference never runs in the trainer. A patched SGLang server owns target parallelism and publishes captured features through Mooncake; every consumer rank is data parallel. Offline runs shard fixed feature references across trainer ranks and may additionally use EAGLE3 USP. See <a href="#parallel-topologies">Parallel topologies</a> for the exact constraints.</p><p>Paths in a config are resolved from the current working directory. The example configs assume that the command is run from the repository root.</p><p>You can override an existing value without copying the YAML. Overrides use validated <code>section.field=value</code> syntax:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/external/qwen3-8b-eagle3-disaggregated.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  training.learning_rate=5e-5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  training.max_steps=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  output_dir=./outputs/eagle3-smoke</span></span></code></pre></div><p>Unknown config fields and unknown override paths are errors. This keeps a misspelled or retired option from being silently ignored.</p><h2 id="run-config" tabindex="-1">Run config <a class="header-anchor" href="#run-config" aria-label="Permalink to &quot;Run config&quot;">​</a></h2><p>A run config has seven typed sections (<code>model</code>, <code>data</code>, <code>training</code>, <code>tracking</code>, <code>profiling</code>, <code>runtime</code>, and <code>deployment</code>) plus <code>run_id</code> and <code>output_dir</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  target_model_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Qwen/Qwen3-8B</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  draft_model_config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">configs/qwen3-8b-eagle3.json</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  target_backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sglang</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  vocab_mapping_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cache/vocab_mapping/qwen3-8b.pt</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  torch_dtype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">bfloat16</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  train_data_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./cache/dataset/sharegpt_train.jsonl</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  max_length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4096</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chat_template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">qwen</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  cache_dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./cache</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">training</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eagle3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  num_epochs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  max_steps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  batch_size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  learning_rate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0e-4</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  save_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">run_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">qwen3-8b-eagle3-disaggregated</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">output_dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">outputs/qwen3-8b-eagle3-disaggregated</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">deployment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">disaggregated</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  trainer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    nnodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    nproc_per_node</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  disaggregated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    control_dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">outputs/qwen3-8b-eagle3-disaggregated/control</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    consumer_state_dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">outputs/qwen3-8b-eagle3-disaggregated/consumer-state</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mooncake</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    server_urls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://127.0.0.1:30000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    mooncake_metadata_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://127.0.0.1:35880/metadata</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    mooncake_master_server_addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">127.0.0.1:35551</span></span></code></pre></div><h3 id="draft-configuration-and-model-initialization" tabindex="-1">Draft configuration and model initialization <a class="header-anchor" href="#draft-configuration-and-model-initialization" aria-label="Permalink to &quot;Draft configuration and model initialization&quot;">​</a></h3><p><code>model.draft_model_config</code> accepts any of these equivalent config sources:</p><ul><li>a local JSON file;</li><li>a local draft-model directory containing <code>config.json</code>;</li><li>a Hugging Face draft-model repository ID.</li></ul><p>It may be omitted for EAGLE3, P-EAGLE, and DFlash. In that case SpecForge derives a registered draft config from the target config. The defaults preserve the former trainers: one EAGLE3 layer, four P-EAGLE layers, and one DFlash layer with block size 16. This DFlash default selects <code>DFlashDraftModel</code>; DFlash2 always requires an explicit draft config or pretrained config source. Typed overrides are available when creating a different fresh architecture:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  target_model_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Qwen/Qwen3-8B</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  draft_num_hidden_layers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # P-EAGLE or DFlash; EAGLE3 remains one layer</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  draft_block_size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # DFlash only</span></span></code></pre></div><p>For DFlash-family drafts, including DFlash2, configure the attention layout in the referenced draft JSON. Each entry corresponds to one draft layer; sliding layers share one positive window:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;num_hidden_layers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;layer_types&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sliding_attention&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sliding_attention&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sliding_attention&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sliding_attention&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;full_attention&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;use_sliding_window&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;sliding_window&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2048</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Use <code>&quot;full_attention&quot;</code> for every entry, <code>&quot;use_sliding_window&quot;: false</code>, and <code>&quot;sliding_window&quot;: null</code> for a full-only draft. The layout length must equal <code>num_hidden_layers</code>. A layer-count override may resize a uniform layout, but a mixed layout must be edited explicitly in the draft JSON.</p><p>The <code>eager</code>, <code>sdpa</code>, and <code>flex_attention</code> backends support both layouts.</p><h3 id="dflash2" tabindex="-1">DFlash2 <a class="header-anchor" href="#dflash2" aria-label="Permalink to &quot;DFlash2&quot;">​</a></h3><p>DFlash2 is a draft-architecture variant of DFlash, not a separate capture strategy. Keep <code>training.strategy: dflash</code> and select it with a draft config whose architecture is <code>DFlash2DraftModel</code>. The target server still captures the same selected hidden states with <code>--spec-capture-method dflash</code>. Online capture also retains the target&#39;s final hidden state for teacher-alignment telemetry; existing offline DFlash datasets remain compatible and omit only the teacher-only metrics.</p><p>The DFlash2 config additionally defines <code>conv_kernel_size</code> and <code>conv_group_size</code> for the local convolution, plus <code>selector_rank</code> and <code>selector_top_k</code> for candidate-path selection. The base draft head uses the configured CE/LK/TV objective. The selector always uses categorical CE over the target head&#39;s strict unary top-k, exactly as it will be used during inference. If the gold token is outside that candidate set, the token contributes no selector loss; <code>selector_coverage</code> reports how often the gold token is present. Both objectives receive the configured fixed-decay or D-PACE position weight, and their combined numerator is normalized by the sum of valid effective token weights rather than by batch or anchor count.</p><p>Set <code>training.dflash2_selector_loss_alpha</code> to scale the selector objective. <code>training.dflash2_selector_warmup_ratio</code> keeps that scale at zero for an initial fraction of optimizer steps, and <code>training.dflash2_selector_ramp_ratio</code> then ramps it linearly to the configured value. Both schedule ratios default to zero. A configured alpha of zero disables selector training and freezes its parameters; a positive alpha keeps the selector in the optimizer and autograd graph even while its effective warmup weight is zero. The convolution starts from an identity kernel with a zero-initialized dynamic projection, and the selector starts as a unary no-op, so both additions initially preserve the corresponding DFlash computation.</p><p>Set <code>training.dflash2_selector_stop_gradient: true</code> to keep the selector CE from updating the unary logits and draft hidden states. The selector parameters still train, and the primary DFlash/D-PACE/LK objective keeps its normal draft gradient path. The option defaults to <code>false</code>, preserving coupled training.</p><p>External tracking reports DFlash-family diagnostics on optimizer steps that reach <code>training.log_interval</code>. Aggregates live under <code>train/dflash/*</code> for every DFlash-family draft and under <code>train/dflash2/selector/*</code> for drafts with a candidate selector. The same families are broken down per predicted block position under their own <code>position_1/*</code> through <code>position_&lt;block_size-1&gt;/*</code> sections (the verified anchor is omitted), so each position renders as one dashboard group:</p><ul><li><code>dflash/hard_label/*</code>: unary top-1 accuracy, top-K recall and probability mass, and gold-token probability. K is the selector&#39;s <code>selector_top_k</code> when present, otherwise the model&#39;s <code>metric_top_k</code> (default 16). Two aggregate-only per-block accepted-length proxies apply <code>1 + sum_k prod_{j&lt;=k} a_j</code> over the supervised slots with <code>a_j</code> being coverage by the unary top-K (<code>unary_topK_oracle_accepted_length</code>) or the gold-token probability (<code>expected_accepted_length</code>, the smooth D-PACE surrogate).</li><li><code>position_&lt;k&gt;/objective/loss_weight_share</code>: the fraction of the effective objective weight each block position receives under the configured fixed-decay or D-PACE weighting.</li><li><code>train/objective/lk_kl_weight</code>: the CE weight of the <code>lambda</code> LK objective.</li><li><code>dflash/teacher/*</code> (online capture only): full-vocabulary expected acceptance, unary top-1 agreement, and unary top-K teacher mass against the frozen target head, plus the aggregate <code>expected_accepted_length</code> that chains the per-slot expected acceptance, the sampling-regime counterpart of the hard-label surrogate.</li><li><code>dflash2/selector/*</code> (DFlash2 only): selector loss, conditional accuracy (uniform over covered slots, so comparable across loss types), the realized greedy serving path&#39;s per-slot accuracy, its per-block <code>serving_accepted_length</code>, and, with online capture, its agreement with the target argmax (<code>teacher_serving_agreement</code>).</li></ul><p>All accepted-length proxies are teacher-forced on the real prefix and anchor.</p><p>The exported computation and parameter names match the public SGLang DFlash2 contract, including optional <code>output_multiplier</code> and <code>final_logit_softcapping</code> transforms from <code>dflash_config</code>.</p><p>The checked-in Qwen3.6-27B recipe owns a two-GPU local stack: one configured GPU runs the target capture server and another runs the trainer. Update its model/data paths and <code>cuda_visible_devices</code> lists for the local host.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/managed-local/qwen3.6-27b-dflash2-disaggregated.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  model.target_model_path=/path/to/Qwen3.6-27B</span></span></code></pre></div><p>Export the result with <code>specforge export --to hf</code>. Serving requires an SGLang version that includes DFlash2 support (SGLang PR #35371); the serving algorithm name remains <code>DFLASH</code>, and the exported <code>DFlash2DraftModel</code> config enables the new path automatically.</p><p>See the dedicated <a href="./../concepts/DFlash2.html">DFlash2 guide</a> for the complete model-field constraints, offline feature workflow, selector schedule, and serving compatibility boundary.</p><p>Domino and DSpark need their projector/head metadata, so they require an explicit draft config (or a pretrained warm-start source that contains <code>config.json</code>). The old Domino parser exposed an optional config flag, but its no-config branch immediately failed because those required projector fields had no defaults; the unified schema rejects that unusable combination early.</p><p>There are two deliberately separate checkpoint operations:</p><table tabindex="0"><thead><tr><th>Intent</th><th>Config field</th><th>Restored state</th></tr></thead><tbody><tr><td>Continue the same run</td><td><code>training.resume_from</code></td><td>draft weights, optimizer/scheduler, epoch/step/data position, and per-rank RNG</td></tr><tr><td>Initialize a new run from weights</td><td><code>model.draft_checkpoint_path</code></td><td>draft weights only</td></tr></tbody></table><p>A weights-only warm start accepts a Hugging Face model directory/repository or a SpecForge checkpoint directory, <code>training_state.pt</code>, or run root. If the warm source contains <code>config.json</code>, it also supplies the draft architecture unless <code>model.draft_model_config</code> is explicit. Warm start never restores optimizer state, counters, data position, or RNG, and it is mutually exclusive with <code>training.resume_from</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  target_model_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Qwen/Qwen3-8B</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  draft_checkpoint_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./outputs/base/base-step1000</span></span></code></pre></div><p>For an online disaggregated run, the producer may receive the same field. It uses only the warm source&#39;s draft configuration to derive the capture contract; the consumer alone loads the draft weights and optimizer.</p><p>Set exactly one data source:</p><ul><li><code>data.train_data_path</code> for raw conversation or preformatted online data;</li><li><code>data.prompts_path</code> for pre-tokenized online JSONL containing <code>input_ids</code> and <code>loss_mask</code>;</li><li><code>data.hidden_states_path</code> for precomputed offline feature checkpoints.</li></ul><p>The checked-in examples are the canonical starting points:</p><table tabindex="0"><thead><tr><th>Strategy</th><th>Category</th><th>Config</th></tr></thead><tbody><tr><td>EAGLE3</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3-8b-eagle3-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-eagle3-disaggregated.yaml</code></a></td></tr><tr><td>EAGLE3</td><td>Offline colocated</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/offline/colocated/qwen3-8b-eagle3-offline.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-eagle3-offline.yaml</code></a></td></tr><tr><td>EAGLE3</td><td>Offline disaggregated</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/offline/disaggregated/qwen3-8b-eagle3-offline-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-eagle3-offline-disaggregated.yaml</code></a></td></tr><tr><td>P-EAGLE</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3-8b-peagle-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-peagle-disaggregated.yaml</code></a></td></tr><tr><td>DFlash</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3-8b-dflash-online.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-dflash-online.yaml</code></a></td></tr><tr><td>DFlash</td><td>Online disaggregated, managed-local</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/managed-local/qwen3-8b-dflash-1server-dp7-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-dflash-1server-dp7-disaggregated.yaml</code></a></td></tr><tr><td>DFlash</td><td>Offline colocated</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/offline/colocated/qwen3-8b-dflash-offline.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-dflash-offline.yaml</code></a></td></tr><tr><td>DFlash2</td><td>Online disaggregated, managed-local</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/managed-local/qwen3.6-27b-dflash2-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3.6-27b-dflash2-disaggregated.yaml</code></a></td></tr><tr><td>Domino</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3-8b-domino-online.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-domino-online.yaml</code></a></td></tr><tr><td>Domino</td><td>Online disaggregated, managed-local</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/managed-local/qwen3-8b-domino-multiserver-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-domino-multiserver-disaggregated.yaml</code></a></td></tr><tr><td>Domino</td><td>Offline colocated</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/offline/colocated/qwen3-8b-domino-offline.yaml" target="_blank" rel="noreferrer"><code>qwen3-8b-domino-offline.yaml</code></a></td></tr><tr><td>DSpark</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3-4b-dspark-disaggregated.yaml" target="_blank" rel="noreferrer"><code>qwen3-4b-dspark-disaggregated.yaml</code></a></td></tr><tr><td>DSpark</td><td>Offline colocated</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/offline/colocated/qwen3-4b-dspark-offline.yaml" target="_blank" rel="noreferrer"><code>qwen3-4b-dspark-offline.yaml</code></a></td></tr><tr><td>DFlash (Ascend)</td><td>Online disaggregated, external</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/external/qwen3.5-4b-dflash-online-npu.yaml" target="_blank" rel="noreferrer"><code>qwen3.5-4b-dflash-online-npu.yaml</code></a></td></tr><tr><td>MTP (Ascend)</td><td>Online disaggregated, managed-local</td><td><a href="https://github.com/sgl-project/SpecForge/blob/main/examples/configs/online/disaggregated/managed-local/qwen3.5-4b-mtp-disaggregated-npu.yaml" target="_blank" rel="noreferrer"><code>qwen3.5-4b-mtp-disaggregated-npu.yaml</code></a></td></tr></tbody></table><h2 id="online-and-offline-data" tabindex="-1">Online and offline data <a class="header-anchor" href="#online-and-offline-data" aria-label="Permalink to &quot;Online and offline data&quot;">​</a></h2><p>Online training captures target features while the run is active. It uses little disk space but keeps target inference available during training. Offline training reads feature checkpoints generated ahead of time, so only the draft model must fit on the training GPUs at the cost of substantially more storage.</p><table tabindex="0"><thead><tr><th>Mode</th><th>Target during training</th><th>Disk use</th><th>Data config</th></tr></thead><tbody><tr><td>Online</td><td>External or managed-local SGLang capture server</td><td>Low</td><td><code>train_data_path</code> or <code>prompts_path</code></td></tr><tr><td>Offline</td><td>Not loaded by the trainer</td><td>High</td><td><code>hidden_states_path</code></td></tr></tbody></table><p>Prepare raw datasets and offline features as described in <a href="./data_preparation.html">Data Preparation</a>, then update the matching example YAML before launching it.</p><h2 id="supported-combinations" tabindex="-1">Supported combinations <a class="header-anchor" href="#supported-combinations" aria-label="Permalink to &quot;Supported combinations&quot;">​</a></h2><p>The unified runtime supports text training in these combinations:</p><table tabindex="0"><thead><tr><th>Strategy</th><th>Online disaggregated</th><th>Offline colocated</th><th>Offline disaggregated</th></tr></thead><tbody><tr><td>EAGLE3</td><td>Yes, consumer DP</td><td>Yes, DP + USP</td><td>Yes, consumer DP</td></tr><tr><td>DFlash/DFlash2</td><td>Yes, consumer DP</td><td>Yes, DP</td><td>Yes, consumer DP</td></tr><tr><td>Domino</td><td>Yes, consumer DP</td><td>Yes, DP</td><td>Yes, consumer DP</td></tr><tr><td>DSpark</td><td>Yes, consumer DP</td><td>Yes, DP</td><td>Yes, consumer DP</td></tr><tr><td>MTP</td><td>Yes, consumer DP</td><td>Yes, DP</td><td>Yes, consumer DP</td></tr><tr><td>P-EAGLE</td><td>Yes, consumer DP, batch size 1</td><td>No</td><td>No</td></tr></tbody></table><p>Unsupported combinations fail explicitly during config validation or run assembly. In particular:</p><ul><li>VLM training, including Qwen2.5-VL, is not supported. The unified runtime currently accepts text inputs only;</li><li>online evaluation is not supported. Evaluation requires precomputed offline features through <code>data.eval_hidden_states_path</code>;</li><li>attention backends are strategy-specific: EAGLE3 accepts <code>sdpa</code>, <code>flex_attention</code>, <code>fa</code>, or offline <code>usp</code>; P-EAGLE requires <code>flex_attention</code>; DFlash, DFlash2, Domino, and DSpark accept <code>eager</code>, <code>sdpa</code>, or <code>flex_attention</code>; MTP accepts <code>eager</code> or <code>sdpa</code>;</li><li>P-EAGLE requires <code>training.batch_size=1</code> and reuses EAGLE3&#39;s server capture schema;</li><li>offline feature training supports EAGLE3, DFlash, DFlash2, Domino, DSpark, and MTP;</li><li>every online run is disaggregated and uses <code>model.target_backend=sglang</code>; finite runs may omit both step fields so the producer can publish the exact optimizer horizon derived from the prepared prompt plan;</li><li>EAGLE3 offline colocated runs derive and cache a deterministic vocabulary mapping from the feature corpus when <code>model.vocab_mapping_path</code> is empty. EAGLE3 disaggregated runs require an explicit shared mapping so producer and consumer cannot derive different artifacts.</li></ul><p>Step limits, LR/loss horizons, logging, saving, and Domino lambda decay are expressed in completed optimizer updates. Fixed datasets are validated to contain complete accumulation windows, and finite online plans do not train an incomplete final quantum. <code>training.max_steps</code> is a stop cap and, when set without <code>training.total_steps</code>, the fallback optimizer/loss schedule horizon. <code>training.total_steps</code> can describe a longer schedule, but does not by itself stop an online stream. When a finite online run omits both, the producer publishes the exact schedule horizon and the consumer trains to EOF.</p><h2 id="parallel-topologies" tabindex="-1">Parallel topologies <a class="header-anchor" href="#parallel-topologies" aria-label="Permalink to &quot;Parallel topologies&quot;">​</a></h2><p>The launcher creates every process group from the typed run config:</p><ul><li>Online target TP/EP belongs to each external SGLang capture server, not the trainer. Online consumers keep <code>training.tp_size</code> and both SP sizes at 1; every trainer rank receives a disjoint feature stream.</li><li>Offline consumers also keep <code>training.tp_size</code> at 1. Without USP, every trainer rank receives a disjoint reference shard and participates as data parallelism.</li><li>EAGLE3 offline can set <code>training.attention_backend: usp</code> and choose <code>training.sp_ulysses_size</code> and <code>training.sp_ring_size</code>. Their product must be greater than one, USP currently uses <code>training.batch_size: 1</code>, and SP peers share one sequence while draft-DP groups receive disjoint references.</li></ul><p>The world size must be divisible by <code>training.sp_ulysses_size * training.sp_ring_size</code>. Use a shared <code>output_dir</code> for multi-rank checkpoints.</p><h2 id="loader-and-profiling-controls" tabindex="-1">Loader and profiling controls <a class="header-anchor" href="#loader-and-profiling-controls" aria-label="Permalink to &quot;Loader and profiling controls&quot;">​</a></h2><p><code>data.dataloader_num_workers</code> controls ordered background feature materialization. If omitted, the former trainer defaults are retained: EAGLE3/P-EAGLE use four workers and DFlash-family strategies use eight. Set it to zero for fully synchronous loading.</p><p>Enable a bounded, per-rank PyTorch trace without a separate profiler entry:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">profiling</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  start_step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  num_steps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  record_shapes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><p>The window is expressed in completed optimizer steps, works across gradient accumulation and resume, and writes one trace per rank beneath <code>output_dir</code>. An active partial window is finalized when training stops or fails.</p><h2 id="evaluation-and-best-checkpoints" tabindex="-1">Evaluation and best checkpoints <a class="header-anchor" href="#evaluation-and-best-checkpoints" aria-label="Permalink to &quot;Evaluation and best checkpoints&quot;">​</a></h2><p>Offline evaluation is configured through the same YAML:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  hidden_states_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./cache/hidden_states/train</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  eval_hidden_states_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./cache/hidden_states/eval</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">training</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  eval_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span></span></code></pre></div><p>The evaluation source and <code>training.eval_interval</code> must be set together. Online evaluation is not supported; setting <code>data.eval_data_path</code> fails config validation. Offline evaluation uses the same feature reader and collator as training and retains the final partial batch. Metrics are emitted under <code>eval/*</code>.</p><p>The default selection metric is <code>eval/simulated_acc_len</code>. An improvement writes a complete checkpoint and points <code>&lt;run_id&gt;-best</code> at it, even when <code>training.save_interval</code> is zero. <code>&lt;run_id&gt;-latest</code> continues to identify the newest complete checkpoint.</p><h2 id="compact-offline-teacher" tabindex="-1">Compact offline teacher <a class="header-anchor" href="#compact-offline-teacher" aria-label="Permalink to &quot;Compact offline teacher&quot;">​</a></h2><p>Offline text EAGLE3 can project teacher targets in exact vocabulary chunks instead of materializing full-vocabulary fp32 logits:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">training</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  strategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eagle3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compact_teacher</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compact_teacher_chunk_size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4096</span></span></code></pre></div><p>This lowers peak memory without changing the teacher distribution, at the cost of additional projection passes. The option is intentionally rejected for online and non-EAGLE3 runs.</p><h2 id="experiment-tracking" tabindex="-1">Experiment tracking <a class="header-anchor" href="#experiment-tracking" aria-label="Permalink to &quot;Experiment tracking&quot;">​</a></h2><p>Console metrics remain available for every run. Select one optional external backend with the top-level <code>tracking</code> section:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">tracking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  report_to</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">tensorboard</span></span></code></pre></div><p>Accepted values are <code>none</code>, <code>wandb</code>, <code>tensorboard</code>, <code>swanlab</code>, and <code>mlflow</code>. W&amp;B, SwanLab, and MLflow have matching project/run fields in the typed schema; TensorBoard writes beneath <code>output_dir/runs</code>, and SwanLab writes beneath <code>output_dir/swanlog</code>. Optimization and evaluator metrics use <code>train/*</code> and <code>eval/*</code>; throughput and timing counters use the top-level <code>perf/*</code> namespace. Historical <code>ploss*</code>, <code>acceptance_rate*</code>, and <code>target_probability</code> names remain available alongside explicit <code>kl_loss*</code> or <code>lk_loss*</code>, <code>ce_loss</code>, and <code>expected_acceptance*</code> names.</p><h2 id="cuda-rocm-and-ascend-npu" tabindex="-1">CUDA, ROCm, and Ascend NPU <a class="header-anchor" href="#cuda-rocm-and-ascend-npu" aria-label="Permalink to &quot;CUDA, ROCm, and Ascend NPU&quot;">​</a></h2><p>CUDA and ROCm runs use the same YAML and entry point. For ROCm, install the checked-in environment before installing SpecForge:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> requirements-rocm.txt</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-deps</span></span></code></pre></div><p>Use a model/backend combination supported by that PyTorch ROCm environment; HF + SDPA recipes are the portable baseline. PyTorch exposes ROCm devices through its <code>torch.cuda</code> API and distributed runs use NCCL.</p><p>For Ascend, install the vendor-matched PyTorch and <code>torch_npu</code> packages first. The checked-in NPU recipes use an external NPU-compatible SGLang capture server and SDPA consumers. A four-device launch is:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ASCEND_RT_VISIBLE_DEVICES</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0,1,2,3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HCCL_CONNECT_TIMEOUT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7200</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HCCL_EXEC_TIMEOUT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7200</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PYTORCH_NPU_ALLOC_CONF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">expandable_segments:True</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/online/disaggregated/external/qwen3.5-4b-dflash-online-npu.yaml</span></span></code></pre></div><p>The unified launcher supplies rank, world-size, and rendezvous variables. The runtime selects the active device dynamically and uses HCCL when <code>torch_npu</code> is active.</p><h2 id="disaggregated-roles" tabindex="-1">Disaggregated roles <a class="header-anchor" href="#disaggregated-roles" aria-label="Permalink to &quot;Disaggregated roles&quot;">​</a></h2><p>A single-node disaggregated config supervises the SpecForge producer and consumer with one command. This does not make the run colocated: the roles and their data-plane contract remain separate. For an external recipe, the command also does not start Mooncake or SGLang. Split deployments use the same config with <code>--role producer</code> or <code>--role consumer</code>; multi-node consumers add only <code>--node-rank</code> on each host. The optional <code>examples/disagg/run_online.sh</code> and <code>run_offline.sh</code> files are thin delegates, not topology wrappers. See the <a href="./disaggregated_training.html">disaggregated training guide</a> for external-service prerequisites, managed-local ownership, freshness rules, and both launch forms.</p><h2 id="checkpoints-and-resume" tabindex="-1">Checkpoints and resume <a class="header-anchor" href="#checkpoints-and-resume" aria-label="Permalink to &quot;Checkpoints and resume&quot;">​</a></h2><p><code>training.save_interval</code> controls checkpoint frequency and <code>training.max_checkpoints</code> controls rotation. Checkpoints are written beneath <code>output_dir</code>. A completed trainer run always saves its final runtime checkpoint, even when <code>save_interval</code> is zero or the final step is not an interval boundary. The <code>&lt;run_id&gt;-latest</code> symlink resolves to the newest complete checkpoint.</p><p>Offline colocated runs restore draft weights, optimizer/scheduler, epoch/step/data position, and per-rank RNG. Offline disaggregated consumers have the same checkpoint contract. For an offline colocated run, override <code>training.resume_from</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/configs/offline/colocated/qwen3-8b-eagle3-offline.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  training.resume_from=./outputs/qwen3-8b-eagle3-offline/qwen3-8b-eagle3-offline-latest</span></span></code></pre></div><p>For a disaggregated offline resume, reuse the same manifest, feature store, and run id, then invoke <code>specforge train -c run.yaml --role consumer</code> with the checkpoint override; the producer never accepts a resume checkpoint.</p><p>Online disaggregated resume is intentionally consumer-only. Reuse the retained SQLite metadata DB, original channel/inboxes, Mooncake objects, and matching checkpoint; rank 0 verifies that the durable optimizer marker equals the checkpoint step, skips acknowledged refs, and requeues the unacknowledged tail. The producer itself is not restarted or resumed. Optimizer/FSDP checkpoints currently require the same trainer world size; control-plane ref redistribution does not imply optimizer-state resharding.</p><p>Training metrics are printed every <code>training.log_interval</code> steps and forwarded to the configured tracking backend.</p><h2 id="export-a-trained-draft" tabindex="-1">Export a trained draft <a class="header-anchor" href="#export-a-trained-draft" aria-label="Permalink to &quot;Export a trained draft&quot;">​</a></h2><p>Runtime checkpoints contain training state and are not serving model directories. Export the final checkpoint before loading it with SGLang or Transformers.</p><p>For EAGLE3 SGLang serving:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> export</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sglang</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --checkpoint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./outputs/qwen3-8b-eagle3-disaggregated/qwen3-8b-eagle3-disaggregated-latest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --draft-config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configs/qwen3-8b-eagle3.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --output-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./exports/qwen3-8b-eagle3-sglang</span></span></code></pre></div><p><code>--to sglang</code> currently implements the EAGLE3 serving-key contract. Use <code>--to hf</code> for DFlash, DFlash2, Domino, DSpark, and P-EAGLE model directories. For an EAGLE-family self-contained Hugging Face directory, provide the target model as the source of the frozen embedding when it is absent from the runtime checkpoint:</p><p>Serving weight names are a fail-silent boundary in SGLang: an unrecognized key may be skipped while the server still starts. The exporter therefore validates the required <code>fc.weight</code>, <code>norm.weight</code>, <code>lm_head.weight</code>, <code>t2d</code>, and <code>d2t</code> keys and rejects any remaining trainer prefix. <code>LlamaForCausalLMEagle3</code> uses an identity map for its <code>midlayer.*</code> and required keys; <code>embed_tokens.weight</code> is deliberately omitted because serving reuses the target model embedding. A new architecture (including a future MLA draft) must add an explicit, loader-version matched weight map and required-key contract before SGLang export is enabled. The export tests cover key structure and tensor round-trip; loading the result in a real speculative-decoding server and measuring acceptance remains a GPU serving validation step.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">specforge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> export</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --checkpoint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./outputs/qwen3-8b-eagle3-disaggregated/qwen3-8b-eagle3-disaggregated-latest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --draft-config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configs/qwen3-8b-eagle3.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --embedding-source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Qwen/Qwen3-8B</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --output-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./exports/qwen3-8b-eagle3-hf</span></span></code></pre></div><p>Pass <code>--vocab-mapping /path/to/mapping.pt</code> when the checkpoint predates the mapping buffers or when you intentionally need to refresh them.</p><p>MTP is deployed by merging its trained native head back into the target model. The merge command accepts the same runtime checkpoint shapes as the generic exporter (<code>training_state.pt</code>, a step/latest directory, or the run output directory):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/merge_mtp_to_base.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --base-model-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Qwen/Qwen3.5-4B</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --mtp-checkpoint-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./outputs/qwen3.5-4b-mtp/qwen3.5-4b-mtp-latest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --draft-config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configs/qwen3.5-4b-mtp.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --output-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./exports/Qwen3.5-4B-MTP</span></span></code></pre></div><p>An already-exported HF MTP draft directory can also be passed as <code>--mtp-checkpoint-path</code>; in that case its own <code>config.json</code> is used and <code>--draft-config</code> may be omitted.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><h3 id="late-oom-or-non-finite-hidden-states-on-online-runs" tabindex="-1">Late OOM or non-finite hidden states on online runs <a class="header-anchor" href="#late-oom-or-non-finite-hidden-states-on-online-runs" aria-label="Permalink to &quot;Late OOM or non-finite hidden states on online runs&quot;">​</a></h3><p><strong>Symptoms.</strong> An online job starts cleanly, then fails well into training with a CUDA OOM that reports a large &quot;reserved but unallocated&quot; pool and only a few MiB free (for example, unable to serve a 388 MiB request with 58.3 GiB reserved and 34 MiB free). The same fragmentation can also surface first as non-finite target hidden states or as an anchor-sampling error that names the data rather than the allocator.</p><p><strong>Cause.</strong> Online training feeds the draft one micro-batch per step whose rows vary in length, so the caching allocator is asked for a differently sized block almost every step. Without expandable segments, reserved-but-unallocated memory accumulates until a modest allocation fails.</p><p><strong>Fix.</strong> Enable expandable segments before launch:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PYTORCH_CUDA_ALLOC_CONF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">expandable_segments:True</span></span></code></pre></div><p>Set this before chasing data or numeric bugs that match the symptoms above. The Ascend equivalent (<code>PYTORCH_NPU_ALLOC_CONF</code>) is already set in the NPU recipes earlier in this page.</p>`,122)])])}const k=a(n,[["render",l]]);export{g as __pageData,k as default};
