# coding=utf-8
"""E0 shim package — the inference plane moved to top-level ``specforge.inference``
(``rollout_worker`` / ``capture`` / ``adapters`` / ``target_engine``). The modules
here re-export from the new home for one release; import the new paths in new code.
"""
