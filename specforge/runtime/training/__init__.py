# coding=utf-8
"""Shim package — the training plane moved to top-level ``specforge.training``
(``controller`` / ``backend`` / ``strategies``). The modules here re-export from
the new home for one release; import the new paths in new code.
"""
